export type ConstantDiscountType = "percentage" | "fixed";
export type ConstantDiscount = "expired" | "active" | "inactive";

export enum DiscountTypeEnum {
  PERCENTAGE = "percentage",
  FIXED = "fixed",
}
export enum DiscountStatusEnum {
  ACTIVE = "active",
  INACTIVE = "inactive",
  EXPIRED = "expired",
}
